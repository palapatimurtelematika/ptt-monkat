root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/api ./cmd/api"
  bin = "./tmp/api"
  include_ext = ["go", "toml", "env"]
  exclude_dir = ["tmp", "bin"]
  delay = 500
  stop_on_error = true

[misc]
  clean_on_exit = true
